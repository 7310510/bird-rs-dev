global:
  config:
    as:                     65003
    router-id:              10.0.0.3
neighbors:
  - config:
      neighbor-address:     10.0.0.100
      peer-as:              65100
      auth-password:        password
    transport:
      config:
        passive-mode:       false
  - config:
      neighbor-address:     2001:db8::100
      peer-as:              65100
      auth-password:        password
    transport:
      config:
        passive-mode:       false